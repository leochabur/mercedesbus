{% extends 'base.html.twig' %}

{% block title %}MetodoEfectivo{% endblock %}

{% block body %}
    <h1>MetodoEfectivo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ metodo_efectivo.id }}</td>
            </tr>
            <tr>
                <th>Importe</th>
                <td>{{ metodo_efectivo.importe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_finanzas_metodo_efectivo_index') }}">back to list</a>

    <a href="{{ path('app_finanzas_metodo_efectivo_edit', {'id': metodo_efectivo.id}) }}">edit</a>

    {{ include('finanzas/metodo_efectivo/_delete_form.html.twig') }}
{% endblock %}
