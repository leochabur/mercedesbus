<form method="post" action="{{ path('app_finanzas_metodo_efectivo_delete', {'id': metodo_efectivo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ metodo_efectivo.id) }}">
    <button class="btn">Delete</button>
</form>
