{% extends 'base.html.twig' %}

{% block title %}MetodoRetencion{% endblock %}

{% block body %}
    <h1>MetodoRetencion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ metodo_retencion.id }}</td>
            </tr>
            <tr>
                <th>Importe</th>
                <td>{{ metodo_retencion.importe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_finanzas_metodo_retencion_index') }}">back to list</a>

    <a href="{{ path('app_finanzas_metodo_retencion_edit', {'id': metodo_retencion.id}) }}">edit</a>

    {{ include('finanzas/metodo_retencion/_delete_form.html.twig') }}
{% endblock %}
