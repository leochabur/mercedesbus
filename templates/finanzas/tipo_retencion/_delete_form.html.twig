<form method="post" action="{{ path('app_finanzas_tipo_retencion_delete', {'id': tipo_retencion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_retencion.id) }}">
    <button class="btn">Delete</button>
</form>
