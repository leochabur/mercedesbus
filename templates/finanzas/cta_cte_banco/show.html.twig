{% extends 'base.html.twig' %}

{% block title %}CtaCteBanco{% endblock %}

{% block body %}
    <h1>CtaCteBanco</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cta_cte_banco.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ cta_cte_banco.numero }}</td>
            </tr>
            <tr>
                <th>LiberaValores</th>
                <td>{{ cta_cte_banco.liberaValores ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_finanzas_cta_cte_banco_index') }}">back to list</a>

    <a href="{{ path('app_finanzas_cta_cte_banco_edit', {'id': cta_cte_banco.id}) }}">edit</a>

    {{ include('finanzas/cta_cte_banco/_delete_form.html.twig') }}
{% endblock %}
