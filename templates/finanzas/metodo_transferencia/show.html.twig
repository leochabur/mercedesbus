{% extends 'base.html.twig' %}

{% block title %}MetodoTransferencia{% endblock %}

{% block body %}
    <h1>MetodoTransferencia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ metodo_transferencium.id }}</td>
            </tr>
            <tr>
                <th>Importe</th>
                <td>{{ metodo_transferencium.importe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_finanzas_metodo_transferencia_index') }}">back to list</a>

    <a href="{{ path('app_finanzas_metodo_transferencia_edit', {'id': metodo_transferencium.id}) }}">edit</a>

    {{ include('finanzas/metodo_transferencia/_delete_form.html.twig') }}
{% endblock %}
