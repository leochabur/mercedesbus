<form method="post" action="{{ path('app_finanzas_cta_cte_delete', {'id': cta_cte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cta_cte.id) }}">
    <button class="btn">Delete</button>
</form>
