<form method="post" action="{{ path('app_finanzas_metodo_cheque_delete', {'id': metodo_cheque.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ metodo_cheque.id) }}">
    <button class="btn">Delete</button>
</form>
