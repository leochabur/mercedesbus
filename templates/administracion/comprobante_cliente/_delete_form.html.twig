<form method="post" action="{{ path('app_administracion_comprobante_cliente_delete', {'id': comprobante_cliente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comprobante_cliente.id) }}">
    <button class="btn">Delete</button>
</form>
