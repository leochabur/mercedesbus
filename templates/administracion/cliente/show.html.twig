{% extends 'base.html.twig' %}

{% block title %}Cliente{% endblock %}

{% block body %}
    <h1>Cliente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cliente.id }}</td>
            </tr>
            <tr>
                <th>RazonSocial</th>
                <td>{{ cliente.razonSocial }}</td>
            </tr>
            <tr>
                <th>Cuit</th>
                <td>{{ cliente.cuit }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ cliente.activo ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ cliente.direccion }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ cliente.telefono }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ cliente.mail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_administracion_cliente_index') }}">back to list</a>

    <a href="{{ path('app_administracion_cliente_edit', {'id': cliente.id}) }}">edit</a>

    {{ include('administracion/cliente/_delete_form.html.twig') }}
{% endblock %}
