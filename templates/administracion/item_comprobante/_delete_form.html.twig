<form method="post" action="{{ path('app_administracion_item_comprobante_delete', {'id': item_comprobante.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item_comprobante.id) }}">
    <button class="btn">Delete</button>
</form>
