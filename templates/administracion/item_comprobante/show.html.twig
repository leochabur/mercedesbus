{% extends 'base.html.twig' %}

{% block title %}ItemComprobante{% endblock %}

{% block body %}
    <h1>ItemComprobante</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item_comprobante.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ item_comprobante.cantidad }}</td>
            </tr>
            <tr>
                <th>PrecioUnitario</th>
                <td>{{ item_comprobante.precioUnitario }}</td>
            </tr>
            <tr>
                <th>PrecioTotal</th>
                <td>{{ item_comprobante.precioTotal }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ item_comprobante.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_administracion_item_comprobante_index') }}">back to list</a>

    <a href="{{ path('app_administracion_item_comprobante_edit', {'id': item_comprobante.id}) }}">edit</a>

    {{ include('administracion/item_comprobante/_delete_form.html.twig') }}
{% endblock %}
