<form method="post" action="{{ path('app_administracion_articulo_concepto_cliente_delete', {'id': articulo_concepto_cliente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ articulo_concepto_cliente.id) }}">
    <button class="btn">Delete</button>
</form>
