<form method="post" action="{{ path('app_administracion_empresa_grupo_delete', {'id': empresa_grupo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ empresa_grupo.id) }}">
    <button class="btn">Delete</button>
</form>
